import { type Config } from "prettier";

const config: Config = {
    trailingComma: "none",
    tabWidth: 4,
    semi: true,
    singleQuote: false,
    bracketSpacing: true
};

export default config;
